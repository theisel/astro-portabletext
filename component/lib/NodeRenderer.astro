---
import type { Component } from "./types";

interface NodeRendererProps {
  component: Component;
  [key: string]: any;
}

const { component, ...props } = Astro.props as NodeRendererProps;
const Cmp: any = component;
---

<Cmp {...props}><slot /></Cmp>
