---
import type { TextNodeProps } from "astro-portabletext/types";

export type Props = TextNodeProps;

const { node, index } = Astro.props;
---

{
  index === 1 ? (
    <>
      &nbsp;
      <span>{node.text.trim()}</span>
    </>
  ) : (
    node.text
  )
}

<style>
  span {
    color: green;
  }
</style>
